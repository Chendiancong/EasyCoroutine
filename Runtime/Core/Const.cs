namespace EasyCoroutine
{
    public enum WorkerStatus
    {
        Waiting,
        Running,
        Succeed,
        Failed,
    }

    public enum CompletableStatus
    {
        Success,
        Fail
    }
}